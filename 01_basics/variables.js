console.log("Hello")

const CollegeName="NitjSr"; //  Used for Contant VAlues which will never be changed
let studentName= "Vinay Gupta"; // mostly prefered to use because it's lifetime only inside of a block
var CourseName= "MCA";                   // Not Used in Recent Time because of its global scope


Interest="Spirituality";  // the way of declaring variable in this way not suitable to code

let a;
var b;
const c="1"; // it can't be declared, must need to initialize
console.log(a)
console.log(b)
console.log(c)

